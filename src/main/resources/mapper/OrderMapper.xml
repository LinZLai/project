<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.mapper.OrderMapper">

    <select id="getIcome" resultType="java.lang.Integer">
        SELECT SUM(T.order_money)
        FROM `t_order` AS T
        WHERE T.`year` = #{year}
        GROUP BY T.`month`
    </select>
    <select id="getIcomeEveryYear" resultType="java.lang.Integer">
        SELECT SUM(T.order_money)
        FROM `t_order` AS T
        WHERE T.`year` IN (#{year}, #{lastYear}, #{beforeLastYear})
        GROUP BY T.`year`
    </select>
</mapper>
